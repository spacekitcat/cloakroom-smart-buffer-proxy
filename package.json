{
  "name": "cloakroom-smart-buffer-proxy",
  "version": "1.3.0",
  "description": "A smart proxy for Node.js Buffer instances that provides and keeps track of references to specific subsets of a Buffer.",
  "main": "index.js",
  "repository": "https://github.com/spacekitcat/buffer-smart-reference-proxy",
  "author": "Lisa Burton",
  "license": "MIT",
  "files": [ "index.js", "lib"],
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.2.2",
    "@babel/preset-env": "^7.3.1",
    "babel-jest": "^24.0.0",
    "jest": "^24.0.0"
  },
  "scripts": {
    "test": "jest --coverage",
    "prebuild": "babel src --out-dir lib --source-maps",
    "build": "npm run test",
    "prepublish": "npm run build"
  },
  "dependencies": {}
}
